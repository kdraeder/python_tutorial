
from setuptools import setup
# only used in this use case.

setup(
    name="mysci",
    version="1.0.0",
    description="a sample package",
    author="kdr",
    author_email="raeder@ucar.edu",
    packages=["mysci"],
    install_requires=[])
# install_requires: Dependencies on non-default packages
